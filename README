# prae 

prae is a cli which generates scaffold code for golang.

# Install 

```bash
go install github.com/ryanreadbooks/prae
```

# Usage

1. use `prae init` to generate the project configuration yml file.

```bash
prae init demo
```